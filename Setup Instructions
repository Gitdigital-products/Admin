1. Create the directory structure and all files as shown above
2. Install dependencies:

```bash
npm install
```

1. Start the development server:

```bash
npm run dev
```

1. Build for production:

```bash
npm run build
```
